# Lifecycle

Single chapter loop:

```text
chapter.md
  -> chapter analyze
  -> chapter suggest
  -> review apply
  -> projection apply
  -> context refresh
```

Outline loop:

```text
reasoning or user goal
  -> outline propose
  -> outline promote
  -> projection apply
```

Projection only consumes:

1. accepted change requests
2. applied draft proposals

